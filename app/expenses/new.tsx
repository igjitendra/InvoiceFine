import{useState}from'react';import{Alert,StyleSheet,Text}from'react-native';import{useRouter}from'expo-router';import{Button}from'@/components/ui/Button';import{Card}from'@/components/ui/Card';import{Input}from'@/components/ui/Input';import{ScreenContainer}from'@/components/ui/ScreenContainer';import{strings}from'@/constants/strings';import{theme}from'@/constants/theme';import{createExpense}from'@/db/repositories/expenses';import{parseRupeesToPaise}from'@/lib/currency';export default function Screen(){const router=useRouter(),[category,setCategory]=useState(''),[date,setDate]=useState(new Date().toISOString().slice(0,10)),[amount,setAmount]=useState(''),[payee,setPayee]=useState(''),[notes,setNotes]=useState(''),[saving,setSaving]=useState(false);async function save(){const paise=parseRupeesToPaise(amount);if(!paise||!category.trim())return Alert.alert(strings.expenses.errorTitle);setSaving(true);try{await createExpense({category,expenseDate:date,amountPaise:paise,payee:payee.trim()||null,notes:notes.trim()||null});Alert.alert(strings.expenses.saved);router.back()}catch{Alert.alert(strings.expenses.errorTitle)}finally{setSaving(false)}}return <ScreenContainer keyboardAware contentContainerStyle={styles.content}><Text style={styles.title}>{strings.expenses.add}</Text><Card style={styles.card}><Input label={strings.expenses.category} value={category} onChangeText={setCategory}/><Input label={strings.expenses.date} helperText={strings.expenses.dateHelp} value={date} onChangeText={setDate}/><Input label={strings.expenses.amount} value={amount} onChangeText={setAmount} keyboardType="decimal-pad"/><Input label={strings.expenses.payee} value={payee} onChangeText={setPayee}/><Input label={strings.expenses.notes} value={notes} onChangeText={setNotes} multiline/></Card><Button label={strings.expenses.save} loading={saving} onPress={()=>void save()}/></ScreenContainer>}const styles=StyleSheet.create({content:{gap:theme.spacing[4]},title:{...theme.typography.screenTitle},card:{gap:theme.spacing[4]}});
