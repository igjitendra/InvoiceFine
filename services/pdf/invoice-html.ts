import type { InvoicePdfData } from '@/types/invoice-pdf';
const e=(v:string|null|undefined)=>String(v??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]??c));const money=(v:number)=>`₹${(v/100).toFixed(2)}`;const qty=(v:number)=>(v/1000).toFixed(3).replace(/\.?0+$/,'');
export function createInvoiceHtml(d:InvoicePdfData):string{const size=d.pageSize==='a4'?'A4':'4in 6in';const compact=d.pageSize==='4x6';const rows=d.lines.map((l,i)=>`<tr><td>${i+1}</td><td>${e(l.description)}${l.sku?`<small>${e(l.sku)}</small>`:''}</td><td>${qty(l.quantityScaled)} ${e(l.unit)}</td><td>${money(l.unitPricePaise)}</td><td>${l.gstRateBasisPoints/100}%</td><td>${money(l.lineTotalPaise)}</td></tr>`).join('');return `<!doctype html><html><head><meta charset="utf-8"><style>@page{size:${size};margin:${compact?'5mm':'12mm'}}*{box-sizing:border-box}body{font-family:Arial,sans-serif;color:#111827;font-size:${compact?'9px':'11px'};margin:0}header{display:flex;justify-content:space-between;border-bottom:2px solid #2563eb;padding-bottom:8px}h1{margin:0;color:#2563eb;font-size:${compact?'18px':'24px'}}h2{margin:0 0 4px}.meta{text-align:right}.parties{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:10px 0}.box{border:1px solid #d1d5db;border-radius:6px;padding:7px}table{width:100%;border-collapse:collapse}th,td{border-bottom:1px solid #d1d5db;padding:${compact?'4px 2px':'6px 4px'};text-align:right}th:nth-child(2),td:nth-child(2){text-align:left}small{display:block;color:#6b7280}.totals{margin:10px 0 0 auto;width:${compact?'65%':'45%'}}.totals div{display:flex;justify-content:space-between;padding:3px}.grand{font-weight:bold;border-top:2px solid #111827;font-size:1.15em}.notes{margin-top:10px}.footer{margin-top:12px;text-align:center;color:#6b7280}</style></head><body><header><div><h1>${e(d.businessName)}</h1><div>${e(d.businessAddress)}</div><div>${e(d.businessPhone)}</div>${d.businessGstin?`<div>GSTIN: ${e(d.businessGstin)}</div>`:''}</div><div class="meta"><h2>INVOICE</h2><b>${e(d.invoiceNumber)}</b><div>Date: ${e(d.invoiceDate)}</div>${d.dueDate?`<div>Due: ${e(d.dueDate)}</div>`:''}</div></header><section class="parties"><div class="box"><b>Bill to</b><div>${e(d.customerName)||'Cash customer'}</div><div>${e(d.customerAddress)}</div>${d.customerGstin?`<div>GSTIN: ${e(d.customerGstin)}</div>`:''}</div><div class="box"><b>Page format</b><div>${d.pageSize==='a4'?'A4':'4 × 6 inch'}</div></div></section><table><thead><tr><th>#</th><th>Item</th><th>Qty</th><th>Rate</th><th>GST</th><th>Amount</th></tr></thead><tbody>${rows}</tbody></table><div class="totals"><div><span>Subtotal</span><span>${money(d.subtotalPaise)}</span></div>${d.discountPaise?`<div><span>Discount</span><span>-${money(d.discountPaise)}</span></div>`:''}${d.cgstPaise?`<div><span>CGST</span><span>${money(d.cgstPaise)}</span></div>`:''}${d.sgstPaise?`<div><span>SGST</span><span>${money(d.sgstPaise)}</span></div>`:''}${d.igstPaise?`<div><span>IGST</span><span>${money(d.igstPaise)}</span></div>`:''}<div><span>Rounding</span><span>${money(d.roundingPaise)}</span></div><div class="grand"><span>Total</span><span>${money(d.totalPaise)}</span></div></div>${d.notes?`<div class="notes"><b>Notes:</b> ${e(d.notes)}</div>`:''}<div class="footer">Generated by InvoiceFine</div></body></html>`;}
