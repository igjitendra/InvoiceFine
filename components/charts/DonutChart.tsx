import Svg,{Circle,G,Text as SvgText}from'react-native-svg';import type{ChartDatum}from'@/types/chart';import type{ChartPalette}from'./palette';export function DonutChart({data,palette,onSelect,centerLabel}:{data:ChartDatum[];palette:ChartPalette;onSelect?:(item:ChartDatum)=>void;centerLabel:string}){const total=data.reduce((n,x)=>n+Math.max(0,x.value),0);if(!total)return null;const radius=48,c=2*Math.PI*radius;let offset=0;return <Svg accessibilityLabel={`${centerLabel} donut chart`} width={148} height={148} viewBox="0 0 148 148"><G rotation="-90" origin="74,74">{data.map((item,i)=>{const len=item.value/total*c,current=offset;offset+=len;return <Circle key={item.id} accessible accessibilityLabel={`${item.label} ${Math.round(item.value/total*100)} percent`} onPress={()=>onSelect?.(item)} cx="74" cy="74" r={radius} fill="none" stroke={item.color??palette.colors[i%palette.colors.length]} strokeWidth="20" strokeDasharray={`${len} ${c-len}`} strokeDashoffset={-current}/>})}</G><SvgText x="74" y="71" fill={palette.text} fontSize="15" fontWeight="700" textAnchor="middle">{data.length}</SvgText><SvgText x="74" y="88" fill={palette.muted} fontSize="10" textAnchor="middle">{centerLabel}</SvgText></Svg>}
