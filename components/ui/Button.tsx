import{ActivityIndicator,Pressable,StyleSheet,Text,type PressableProps}from'react-native';import{theme}from'@/constants/theme';import{useAppPalette}from'@/hooks/useAppPalette';type Variant='primary'|'secondary'|'danger';type Props=Omit<PressableProps,'children'|'style'>&{label:string;loading?:boolean;variant?:Variant};export function Button({disabled,label,loading=false,variant='primary',...props}:Props){const p=useAppPalette(),off=disabled||loading;const background=variant==='primary'?p.primary:variant==='danger'?p.danger:p.surface;const border=variant==='secondary'?p.border:background;const color=variant==='secondary'?p.primary:'#FFFFFF';return <Pressable accessibilityRole="button" accessibilityState={{disabled:off,busy:loading}} disabled={off} style={({pressed})=>[styles.base,{backgroundColor:background,borderColor:border},pressed&&!off&&styles.pressed,off&&styles.disabled]} {...props}>{loading?<ActivityIndicator color={color}/>:<Text style={[styles.label,{color}]}>{label}</Text>}</Pressable>}const styles=StyleSheet.create({base:{minHeight:52,minWidth:44,paddingHorizontal:16,alignItems:'center',justifyContent:'center',borderRadius:14,borderWidth:1},pressed:{opacity:.86,transform:[{scale:.99}]},disabled:{opacity:.55},label:{...theme.typography.button}});
