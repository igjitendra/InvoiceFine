import{useRef}from'react';import{Animated,Pressable,type PressableProps,type StyleProp,type ViewStyle}from'react-native';type Props=Omit<PressableProps,'style'>&{style?:PressableProps['style'];wrapperStyle?:StyleProp<ViewStyle>;scaleTo?:number};export function PressableScale({onPressIn,onPressOut,scaleTo=.97,style,wrapperStyle,...props}:Props){const scale=useRef(new Animated.Value(1)).current;function move(toValue:number){Animated.spring(scale,{toValue,useNativeDriver:true,speed:28,bounciness:4}).start()}return <Animated.View style={[wrapperStyle,{transform:[{scale}]}]}><Pressable style={style} onPressIn={event=>{move(scaleTo);onPressIn?.(event)}} onPressOut={event=>{move(1);onPressOut?.(event)}} {...props}/></Animated.View>}
